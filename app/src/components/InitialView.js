function InitialView( {view} ) {
    return (
        <>
        <h2 id='initial-view' className="text-center fontcolor">Hungry? Click on an option above to check out our potato filled menu!</h2>
        
        </>
    );
  }

export default InitialView;